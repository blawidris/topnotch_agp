<script src="//www.google.com/recaptcha/api.js" ></script>
<fieldset>
  <legend>{{ text_captcha }}</legend>
  <div class="form-group required">{% if route|slice(0, 9) == 'checkout/' %}
    <label for="input-payment-captcha">{{ entry_captcha }}</label>
    <div id="input-payment-captcha" class="g-recaptcha" data-sitekey="{{ site_key }}"></div>
    {% if error_captcha %}
    <div class="invalid-feedback d-block">{{ error_captcha }}</div>
    {% endif %}
    {% else %}
    <div class="row">
    <label class="col-sm-2 col-form-label">{{ entry_captcha }}</label>
    <div class="col-sm-10">
      <div class="g-recaptcha" data-sitekey="{{ site_key }}"></div>
      {% if error_captcha %}
      <div class="invalid-feedback d-block">{{ error_captcha }}</div>
      {% endif %}</div>
    </div>
    {% endif %}
    </div>
</fieldset>
