<div class="contact-form">
  {% if heading_title %}
  <h4 class="widget-title contact-form-title">{{ heading_title }}</h4>
  {% endif %}
  <form action="{{ action }}" onsubmit="mz_sendMessage(this);return false;" method="post">
    <div class="form-group"><input type="text" name="name" placeholder="{{ text_name }}" class="form-control form-control-{{ size }}" /></div>
    <div class="form-group"><input type="text" name="email" placeholder="{{ text_email }}" class="form-control form-control-{{ size }}" /></div>
    <div class="form-group"><input type="text" name="subject" placeholder="{{ text_subject }}" class="form-control form-control-{{ size }}" /></div>
    <div class="form-group"><textarea name="message" placeholder="{{ text_message }}" class="form-control form-control-{{ size }}"></textarea></div>
    <input type="hidden" name="entry_id" value="{{ entry_id }}" />
    {{ captcha }}
    <button type="submit" data-loading="{{ text_loading }}" class="btn btn-{{ color }} btn-{{ size }}">{{ text_submit }}</button>
  </form>
</div>
